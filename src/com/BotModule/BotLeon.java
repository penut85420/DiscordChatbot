package com.BotModule;

import com.Analysis.Acting.*;

public class BotLeon {
	@SuppressWarnings("unused")
	public String Fooo(String msg) {
		// Msg input
		ActingManager am = new ActingManager();
		// ActingMatch m = am.match(msg);
		// String type = ResponseManager.getResponseType(m.getPatternType);
		// if (type != null)
		// return ResponseManager.getResponse(type, m.getMatchers, 5);
		
		// Pattern match, get unknowns objects
		// Make Matchers, Ex: matchers.add("game", "踩地雷");
		// Send pattern type and matchers to response
		// ResponseManager.getResponse(patternType, emotionNumber, matchers);
		// Return response
		
		return null;
	}
}
